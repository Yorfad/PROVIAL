# =============================================
# CONFIGURACIÓN DE PRODUCCIÓN - PROVIAL API
# =============================================
# IMPORTANTE: Cambiar TODOS los valores antes de usar en producción

# Server
NODE_ENV=production
PORT=3000
API_PREFIX=/api

# Database (Cambiar por credenciales de producción)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/provial_db
DB_HOST=your-db-host.com
DB_PORT=5432
DB_NAME=provial_db
DB_USER=provial_user
DB_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO

# Redis (Cambiar por instancia de producción)
REDIS_URL=redis://your-redis-host:6379
REDIS_HOST=your-redis-host
REDIS_PORT=6379

# JWT (OBLIGATORIO: Generar claves seguras)
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIAR_POR_CLAVE_SEGURA_DE_64_BYTES_MINIMO
JWT_REFRESH_SECRET=CAMBIAR_POR_OTRA_CLAVE_SEGURA_DIFERENTE
JWT_EXPIRES_IN=12h
JWT_REFRESH_EXPIRES_IN=7d

# CORS (Dominios permitidos en producción)
CORS_ORIGIN=https://tu-dominio.com,https://app.tu-dominio.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Socket.io
SOCKET_IO_CORS_ORIGIN=https://tu-dominio.com,https://app.tu-dominio.com

# File Upload
MAX_FILE_SIZE_MB=10
UPLOAD_DIR=/app/uploads

# Firebase (para notificaciones push)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_PRIVATE_KEY=your-private-key
# FIREBASE_CLIENT_EMAIL=your-client-email
